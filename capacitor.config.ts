import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tellevoapp_2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
